内存模型。图示：java-memory-model-1.png

每个运行在java虚拟机的线程都有自己的线程栈。这个线程栈包含了这个线程调用的方法当前执行点的相关信息。
一个线程仅能访问自己的线程栈。一个线程创建的本地变量对其他线程不可见，仅自己可见。
所有原始类型的本地变量都存放在线程栈上，因此对其他线程不可见，一个线程可能向另一个线程传递一个原始类型变量的拷贝，但是不能共享这个原始变量本身。
堆上包含了java程序中创建的所有对象

图示：java-memory-model-2.png

一个原始类型的本地变量，保存在线程栈上。对象引用，引用保存在线程栈上，对象放在堆上。








